# Examples

Use this directory for sanitized, shareable examples only.

- Good candidates: demo imports, generated sample reports, documentation assets, or reproducible fixtures.
- Bad candidates: real bank exports, live databases, API keys, or anything copied from `data/`, `inbox/`, or `processed/`.

Runtime folders should stay local; examples should stay portable.
