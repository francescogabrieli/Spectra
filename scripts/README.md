# Scripts

Launcher implementation lives in `scripts/launchers/`.

The repository root keeps thin wrappers only:

- `./spectra`
- `.\spectra.ps1`
- `spectra.cmd`

This preserves a simple user experience while keeping the root directory clean.
